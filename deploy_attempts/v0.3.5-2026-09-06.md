# v0.3.5 deploy attempt — 2026-09-06

**Deploy tx**: `0x66969a6acca85b0e3606ce4dce8e8ef133ba5dff626fbd095f7427d4ec54ac68`
**Deployer**: `0x08876A74135586bD04d758Dc50a9b68a934Aebc1` (provenance-deployer)
**New address**: `0x1C5DB1D42DC15181ca8eA3235beB3a12b02892FA`
**Tx status**: SUCCESS (status 0x1, ACCEPTED, 3 AGREE votes)

**Why it didn't actually deploy**:
- `set_api_keys` write tx on the new contract: `result_name: NO_MAJORITY`, ACCEPTED at leader
- First `register_artist` tx on new contract: leader errored, `node_config.address: "contract_not_found_handler"`
- Schema/contract-call RPCs: "Contract 0x1C5DB1D... not found"
- Studio deployment pipeline: `DEPLOYMENT_PAUSED` (per the explorer API)

The deploy tx is recorded on-chain but the consensus nodes don't have the contract
code. The leader's "ACCEPTED" was a no-op consensus — the validators all saw
"contract not found" and voted ACCEPTED with empty receipts.

**Fix needed**: studio deployment pipeline has to be un-paused before new deploys
work. Until then, stay on v0.3.4 at `0x703AdAB82751A9006aFE9c477DC344f8D9CA4384`.

**v0.3.5 source changes** (committed in `d20a536`):
- `_apple_music_search` splits artistName on `,`, `&`, `feat.`, `featuring`,
  `with`, `x` and requires exact segment match before falling back to substring.
- Substring fallback no longer sets `track_present=True` (co-artist-only tracks
  don't bump strict score).
- Confirmed: `Four Tet` now resolves to `35888604` (correct) instead of `356545647` (Skrillex, was wrong).

Once the studio pipeline resumes, redeploy with:
```
genlayer deploy --contract contracts/ProvenanceRegistry.py --rpc https://studio.genlayer.com/api --args 0xb7278A61aa25c888815aFC32Ad3cC52fF24fE575
```
(should not need GEN — `provenance-deployer` had 0 balance but deploy "succeeded" via the disabled pipeline)
